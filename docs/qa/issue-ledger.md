# Issue ledger

Baseline: `2cd8818b057b26c59f3ec95b5763d42783b2cb64`. Changes are local on `codex/nexuside-qa`; the published release and original checkout were preserved. P0 denotes possible data loss or unintended execution, P1 a broken core workflow, P2 substantial usability/coverage friction. “Fixed” is limited to the stated reproduction and regression checks.

| ID / severity | Origin and reproduction | Expected / observed; cause | Fix and regression evidence | Status |
|---|---|---|---|---|
| D01 / P0 | Baseline: retain the edits approval A; another client approves A, then submit the retained decision after tests approval B appears. | Only A may be authorized; the baseline executed B's host tests. Decisions identified the run but not the exact pending action. | SHA-256 revision binds run, action and saved configuration; validation occurs inside the engine lock. Q01/Q02, browser A01, installed CLI C01. Baseline `reproductions.json` records `testExecuted: true`. | Fixed |
| D02 / P0 | Baseline: type in a file, select another file and return. | Unsaved text should survive; the baseline textarea lost it. Editing state belonged to a disposable view. | Persistent CodeMirror document model, independent view positions, shared history/content, dirty last-view close, recovery sessions. E01/E02/E04/E05; actual installed webview save. | Fixed |
| D03 / P0 | Concurrent manual save and approved edit target the same before-content. | At most one conflicting write may succeed. Separate unguarded read/write sequences could overwrite a competing update. | Shared filesystem lock, compare-and-swap under lock, atomic replacement, identical-content success. Q03 and core conflict test; E03/E05 verify real files. | Fixed |
| D04 / P0 | Simulate interruption between writes of a multi-file proposal. | Recover or expose conflict without silently leaving an untracked partial transaction. There was no durable transaction journal. | Journal before mutation; recovery checks every before/after content, rolls back safe partial writes, preserves conflicting evidence. Q07 deterministic interrupted-state fixture. Physical power loss and fsync durability are unverified. | Fixed within stated boundary |
| D05 / P1 | Baseline: switch IDE/Agent. | Two distinct workflows; the toggle primarily changed request mode while retaining the same task-centric interface. | Explorer, CodeMirror tabs/groups and contextual assistance in IDE; task/composer/review/history/trace in Agent. Drafts survive switching; existing run mode/config remain immutable. E01, Q08 and before/after screenshots. | Fixed |
| D06 / P1 | Baseline editor lacks multiple document/group interactions. | Essential navigation, undo and splits must work without replacing buffers. | CodeMirror 6, tab reorder, edge splits, group move/focus/resize/close, synchronized document views, keyboard alternatives and explicit bounds. E01/E04/E06/E09/E11/S02. | Implemented; finite coverage |
| D07 / P1 | Delay a save, type new text, then repeat Save. | The earlier save must not mark later keystrokes saved or overwrite them. | Save captures exact content/before revision; later typing remains dirty, duplicate saves serialize. E05 verifies disk and buffer independently. | Fixed |
| D08 / P1 | External edit or deletion while the document is dirty. | User must see a conflict and review the disk version before replacing it. | Clean-buffer reload, dirty conflict banner/dialog, explicit keep-mine compare-and-swap and deleted-file recreation. E03/E10, Q03. | Fixed |
| D09 / P2 | Create, rename or delete files/folders; retry with a stale preview. | Real filesystem operations with safeguards and a recoverable delete. | Nested/Unicode paths, bounded subtree revision, refusal of existing destinations/sensitive paths/links, recovery trash plus Undo. E03/E10/Q04. POSIX permission denial is verified by E14; Windows ACL/max-path cases remain unverified. | Implemented |
| D10 / P1 | New editor initially reused a group's changing active path for delayed view transactions. | A transaction belongs to the view/document that created it. | Dispatch binds the exact source view and path. E01/E04 and seeded S02 exercise repeated switching. | Introduced during work; fixed |
| D11 / P2 | Navigate to line 150 in a long file, switch files and return. | Restore scroll position; an intermittent run returned to scrollTop 0. A deferred transaction from the destroyed previous view overwrote its saved position with zero; diagnostic traces proved the obsolete view still dispatched after unmount. | Reject dispatch from any view no longer registered to that group; restore after CodeMirror measurement. E11 retains the failing assertion; ten fresh-fixture repetitions pass. `qa-keyboard-final.log` preserves the pre-fix failure. | Introduced during work; fixed |
| D12 / P1 | First full 30-minute soak, repeatedly submit/select local runs. | At most one run poll in flight; observed maximum was two. Run-selection effects could overlap a previous request. | A shared request promise serializes transitions and ignores stale results; terminal runs stop polling. A02 deterministic held-response regression. Initial failed 1,801-second soak retained; final confirmation is in the QA report. | Fixed; soak result reported separately |
| D13 / P1 | Decode an allowed near-20 MB media attachment. | Accept within limit without crashing. Nested repeated base64 regex groups exhausted the JS stack. | Linear base64 validation, length and decoded-byte limits. Q09 accepts exact bound and rejects invalid/over-bound input. Failure and corrected 32-test logs retained. | Introduced during work; fixed |
| D14 / P1 | Open invalid UTF-8 bytes as editable text. | Reject unsupported encoding without silently replacing bytes with replacement characters. | Fatal UTF-8 decoding, separate binary and byte-size errors. Q05/E08; UTF-8/emoji/combining/CRLF preservation E07. | Fixed |
| D15 / P1 | Double-submit the same task identifier from separate clients. | One persisted run. UI disabling alone cannot coordinate clients. | UUID idempotency persisted under the shared lock; synchronous UI submission guard. Q06. | Fixed |
| D16 / P1 | SIGINT during provider work; Ctrl+C at an interactive approval prompt. | Persist cancellation and exit 130. The approval prompt initially exited 1 and retained awaiting_approval because readline consumed terminal Ctrl+C. | Readline SIGINT aborts the prompt, awaits engine cancellation and reloads final state; cancellation exit status takes precedence. C04 and actual POSIX PTY approval/rejection/cancellation tests. | Fixed |
| D17 / P2 | Request invalid CLI flags with --json or run in a Unicode path. | Parseable error JSON and correct workspace recovery command. | Commander errors share JSON output handling; recovery uses engine workspace. Installed C01–C04. Shell metacharacter paths beyond spaces/Unicode are not verified. | Fixed within tested paths |
| D18 / P2 | Hundreds of runs, long traces/diffs and repeated state refresh. | Usable lists, bounded rendering and truthful truncation. | Show-more history, trace/diff pagination, single-flight polling, 5-second idle state refresh; one trace read per run response. S03 and S01. | Fixed within supported limits |
| D19 / P2 | Accessibility scan/editor keyboard navigation. | Correct tab ownership, labels, focus and readable syntax. | Tab/close controls separated, roving tab focus, dialog restoration, improved comment contrast and invalid regex feedback. Axe serious/critical violations: zero on tested IDE state; V01/E07/E09. | Fixed; real AT unverified |
| D20 / P2 | Large workspace literal search and navigation benchmark. | Meet predeclared budgets while respecting the index limit. | Search reads in bounded batches of 16, limits disclosed. Quieter before/after performance evidence retained. Some file-open samples still miss the 300 ms target; CodeMirror plus full indexed explorer rendering adds work. | Partial; performance follow-up |
| D21 / P2 | Provider credential removal. | Removing a session/store credential should not immediately fall back to an already-present environment value in that session. | Explicit removal tombstone and SecretStorage deletion callback; replacement clears tombstone. Browser fixture checks are reported in matrix; actual OS keychain remains unverified. | Implemented; platform verification limited |
| D22 / P1 | Hold the HTTP response for opening file B, select A, then release B. | A later selection should remain active; a slow earlier open stole focus when it completed. | Per-group open intent and focus intent retain the latest selection while adding loaded files as background tabs. E12 holds the real transport response; ten fresh-fixture repetitions pass. | Introduced during work; fixed |
| ENV01 / P2 | First baseline Docker browser integration run. | Network sandbox creation; daemon returned HTTP 500 from /v1.53/networks/create. | Retained baseline failure; later all three sandbox checks passed. Client/server both reported API 1.53, so an API-version mismatch was not established. | Intermittent environment failure; root cause unresolved |
| ENV02 / P2 | Native comment/undo shortcut test. | Actual foreground editor command; macOS reports window.state.focused=false despite CDP DOM focus. | Keep assertion behind real foreground prerequisite; strict opt-in NEXUS_REQUIRE_NATIVE_FOCUS=1 fails when unavailable. Native save/split APIs and webview input checked separately. | Blocked; no false pass |
| ENV03 / P2 | Native zoom screenshot review. | Visible webview at 80–200%. Multiple host editor groups/sidebar/chat imposed minimum widths and crowded the target outside the window; CDP pointer hit an adjacent pane at 80%. | Verify native split first, join host groups and close auxiliary panes for zoom visual checks; assert webview lies inside host window. Actual DPR change proves host zoom. Keyboard checks do not establish physical pointer/shortcut behavior. Earlier host-test logs document the crowded layout; final screenshots show the joined host layout. | Partial; see host report |
| TEST01 / P2 | Additional keyboard test development. | Stable fixtures/assertions. Early checks used a wrong CodeMirror go-to-line class and let transient ENOENT escape polling while a create completed. | Correct observed selector and poll absent file as pending. Assertions still require the created/recovered file and exact content. A later performance harness fix avoided a tsx-injected __name helper crossing into browser scope and waited for actual loaded file contents rather than an empty textarea. Logs remain separate from product defects. | Harness corrected |

See [interaction matrix](interaction-matrix.md) for untested states and [verification commands](README.md). The report outside the repository includes actual pass counts, soak results, screenshots, environment and artifact checksums.
